# Core dependencies
chromadb>=1.10.0
sentence-transformers>=2.0.0
numpy>=1.20.0
requests>=2.25.0

# Optional format support (install with: pip install -r requirements.txt -r requirements-formats.txt)
# See requirements-formats.txt for all format parsers
